spring:
  application:
    name: order-fulfillment
  datasource:
    url: jdbc:postgresql://localhost:5432/operaton
    username: operaton
    password: operaton
operaton:
  bpm:
    admin-user:
      id: demo
      password: demo
      first-name: Demo
    filter:
      create: All tasks

# External service URLs (override for tests via Testcontainers)
inventory:
  service:
    url: http://localhost:8089
payment:
  service:
    url: http://localhost:8089
notification:
  service:
    url: http://localhost:8089
