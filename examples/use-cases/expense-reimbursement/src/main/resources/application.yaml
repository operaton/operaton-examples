spring:
  application:
    name: expense-reimbursement
  datasource:
    url: jdbc:postgresql://localhost:5432/operaton
    username: operaton
    password: operaton
  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}

operaton:
  bpm:
    admin-user:
      id: demo
      password: demo
      first-name: Demo
    filter:
      create: All tasks

llm:
  base-url: ${LLM_BASE_URL:http://localhost:11434}
  api-key: ${LLM_API_KEY:ollama}
  model: ${LLM_MODEL:llama3.2-vision}

mail:
  from: ${MAIL_FROM:reimbursement@example.com}
